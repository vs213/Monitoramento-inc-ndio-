import React from "react";
import ReactDOM from "react-dom/client";
import MapaFocosIncendio from "./MapaFocosIncendio";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <MapaFocosIncendio />
  </React.StrictMode>
);
